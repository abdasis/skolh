import type { ArticleResource, ArticleStats } from '@/types';

interface Props {
    articles: ArticleResource[];
    stats: ArticleStats;
}

const AdminArticlesIndex = (_props: Props) => {
    return null;
};

export default AdminArticlesIndex;
